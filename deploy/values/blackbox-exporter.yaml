resources:
  requests: { cpu: 10m, memory: 32Mi }     # 24h peak 32Mi (hit the old limit)
  limits: { memory: 48Mi }
serviceMonitor:
  enabled: true
  defaults:
    interval: 60s
    module: http_2xx
  targets:
    - name: production
      url: https://szymongrabowski.dev/
    - name: next
      url: https://next.szymongrabowski.dev/
