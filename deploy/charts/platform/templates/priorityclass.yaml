apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: site-critical
  labels:
    {{- include "platform.labels" . | nindent 4 }}
value: 1000000
globalDefault: false
preemptionPolicy: PreemptLowerPriority
description: Public website pods
